CREATE TABLE Currency (
    code TEXT NOT NULL PRIMARY KEY,
    value REAL NOT NULL
);

insertCurrency:
INSERT OR REPLACE INTO Currency(code, value)
VALUES(?, ?);

selectAllCurrencies:
SELECT *
FROM Currency;

removeAllCurrencies:
DELETE FROM Currency;
